This branch was created to fix the sync-logic bug that causes data corruption during power failures. Isolating the fix on this branch prevents unstable code from affecting the main production system.
 
